{
  "name": "compliance-data-layer",
  "version": "0.0.0",
  "private": true,
  "description": "CTC readiness, remediation and validation. Two deployables, forever.",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run"
  },
  "pnpm": {
    "$comment": "esbuild is vitest's bundler and needs its postinstall to place a platform binary. Declared explicitly rather than left to an interactive `pnpm approve-builds`, so a fresh clone and CI behave identically.",
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^26.1.2",
    "eslint": "^9.39.1",
    "openapi-typescript": "^7.9.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.2",
    "vitest": "^3.2.4"
  }
}
